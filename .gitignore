# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Dependency dirs (none used, but just in case)
node_modules/

# Local env files — keep real secrets out of git
.env
.env.*

# Deployment build artifacts (produced by package.bat)
alumni-website-hostinger.zip
_deploy_stage/
